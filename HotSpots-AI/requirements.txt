# Runtime dependencies for the Vercel serverless API
# NOTE: Heavy GIS packages (rasterio, geopandas, osmnx, earthengine-api, geemap)
# are intentionally excluded — they are only needed for one-time data generation,
# not for serving the live API. Including them would exceed Vercel's 250MB limit.
# NOTE: azure-cognitiveservices-speech is excluded because it uses native C++
# binaries that are incompatible with Vercel's serverless Python environment.
fastapi
uvicorn[standard]
python-dotenv
joblib
numpy
scikit-learn
openai
requests
